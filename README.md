# Median-Filter-for-generic-resolution-images
The project is a description of a Median Filter circuit for generic resolution (as long as it is a power of 2). The `median_filter_tb` is used to read the infile and write the outfile from the use of the library "TEXTIO". It was use a  median network to obtain the median of 3x3 window of pixels. 
